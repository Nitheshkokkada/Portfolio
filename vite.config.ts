import tailwindcss from '@tailwindcss/vite';
import react from '@vitejs/plugin-react';
import path from 'path';
import {defineConfig} from 'vite';

export default defineConfig(() => {
  return {
    plugins: [react(), tailwindcss()],
    // SECURITY NOTE: Removed insecure 'define' block for GEMINI_API_KEY and broad 'loadEnv' call.
    // Baking API keys into the production build client bundle exposes them publicly to the browser.
    // Environment variables should be loaded securely on a backend server or proxy if needed.
    resolve: {
      alias: {
        '@': path.resolve(__dirname, '.'),
      },
    },
    server: {
      // HMR is disabled in AI Studio via DISABLE_HMR env var.
      // Do not modify—file watching is disabled to prevent flickering during agent edits.
      hmr: process.env.DISABLE_HMR !== 'true',
    },
  };
});
